# 新增模块与函数说明

本文档逐一说明本次建立的头文件和源文件。`README.md` 用于快速认识项目；本文档用于理解
每个文件为什么存在、公开接口做什么、内部如何实现。

## 代码注释规范

本工程自定义代码采用三层中文注释：

- 文件级注释：说明文件所属层级、职责和依赖边界。
- 函数级 Doxygen 注释：说明用途、参数、返回值、前置条件和关键实现方法。
- 函数内部逐段注释：说明参数检查、状态切换、硬件事务、数据换算、错误处理和输出阶段，重点解释“为什么这样实现”。

注释覆盖 `Core/com`、`Core/bsp`、`Core/dri`、`Core/mid`、`Core/app` 各层 `Inc`、`Src` 中的全部自定义 `.h/.c` 文件。CubeMX 自动生成的 `Core/Inc`、`Core/Src` 以及 ST 官方 `Drivers` 不进行整文件注释改写，只在 USER CODE 区保留必要说明，避免重新生成代码时产生冲突。
## Com 公共层

### `Core/com/Inc/app_status.h`

定义所有自定义层统一使用的 `AppStatus`。这样驱动层不向上暴露 `HAL_StatusTypeDef`，中间层和
应用层也不依赖 STM32 HAL。状态覆盖成功、忙、超时、I/O 错误、参数错误、器件 ID 错误、
回读校验失败和未初始化。

### `Core/com/Inc/com_crc.h`

声明 CRC8 通用数据校验接口，不属于具体 BSP 或器件驱动。
- `ComCrc8Ccitt_Calculate(data, length)`：计算当前 UART 协议使用的 CRC8-CCITT，
  参数为 Poly `0x07`、Init `0x00`、不反转、XorOut `0x00`。

### `Core/com/Src/com_crc.c`

实现逐字节、逐位 CRC8。算法将输入字节异或到 8 位 CRC，再按 bit7 到 bit0
执行 8 次左移和 `0x07` 条件异或；标准测试向量 `123456789` 的结果为 `0xF4`。

## Bsp 板级支持层

### `Core/bsp/Inc/board_config.h`

集中保存从原理图确认的板级常量：三颗 MLX 地址、两颗 IMU 地址、I2C 超时、采样周期和 Flash
配置区边界。器件驱动不应写死这些板级角色。

### `Core/bsp/Src/board_config.c`

定义 `g_board_mlx_addresses` 和 `g_board_imu_addresses` 两张地址表。应用层按数组顺序创建器件
上下文，其中 MLX 索引 0 是主磁传感器，索引 1、2 是干扰参考。

### `Core/bsp/Inc/bsp_i2c.h`

声明板级 I2C 事务接口：

- `BspI2c_Write()`：普通阻塞式主机写。
- `BspI2c_CommandRead()`：写一个命令字节后 repeated START 读取，供 MLX EX/SM/RM 使用。
- `BspI2c_CommandAddressRead()`：写命令和参数后读取，供 MLX RR 使用。
- `BspI2c_CommandWriteReadStatus()`：发送多字节命令段后读取状态，供 MLX WR 使用。
- `BspI2c_MemRead()`：标准 8 位寄存器地址读取，供 LSM6DSOW 使用。
- `BspI2c_MemWrite()`：标准 8 位寄存器地址写入，供 LSM6DSOW 使用。

### `Core/bsp/Src/bsp_i2c.c`

把上述接口绑定到 CubeMX 的 `hi2c1`。

- `map_hal()`：内部函数，把 HAL 返回值映射为 `AppStatus`。
- `BspI2c_Write()`：调用 `HAL_I2C_Master_Transmit()`。
- `BspI2c_CommandRead()`：利用 8 位 `HAL_I2C_Mem_Read()` 生成命令后的 repeated START。
- `BspI2c_CommandAddressRead()`：把命令和参数组合为 16 位 memory address，再读取响应。
- `BspI2c_CommandWriteReadStatus()`：通过 `HAL_I2C_Master_Seq_Transmit_IT()` 与
  `HAL_I2C_Master_Seq_Receive_IT()` 保持总线不 STOP；第一帧完成后 HAL 将状态恢复为 READY 并
  保留上一帧方向，第二帧因此生成 repeated START。每一帧结束后均检查 HAL ErrorCode，避免把
  NACK 或总线错误误判成成功。
- `wait_it_transfer()`：等待顺序中断事务完成；超时后发起 abort 并等待其收尾，降低下一笔事务
  因句柄仍处于 BUSY/ABORT 状态而失败的概率；统一返回超时或 I/O 错误。
- `BspI2c_MemRead()`、`BspI2c_MemWrite()`：直接封装 HAL 标准寄存器事务。

### `Core/bsp/Inc/bsp_time.h`

声明基于 TIM2 的板级时间接口，使驱动层和应用层不直接依赖 STM32 HAL。

- `BspTime_Init()`：清零软件/硬件计数并启动 TIM2 500 Hz 更新中断。
- `BspTime_GetTickMs()`：返回可自然回绕的 32 位系统毫秒计数。
- `BspTime_DelayMs()`：基于 TIM2 tick 提供初始化路径使用的毫秒阻塞延时。
- `BspTime_OnTim2Elapsed()`：由 HAL 定时器回调调用，每 2 ms 将毫秒计数增加 2。

### `Core/bsp/Src/bsp_time.c`

- `g_tim2_tick_ms`：TIM2 500 Hz 中断维护的 `volatile uint32_t` 毫秒计数。
- `BspTime_Init()`：清零计数器、清除更新标志并调用 `HAL_TIM_Base_Start_IT()`。
- `BspTime_GetTickMs()`：原子读取板级毫秒计数。
- `BspTime_DelayMs()`：使用无符号差值等待，兼容 32 位回绕。
- `BspTime_OnTim2Elapsed()`：中断中只执行一次加 2，缩短高优先级 ISR 占用时间。

### `Core/bsp/Inc/bsp_uart_dma.h`

声明 LPUART1 TX DMA 生命周期接口：

- `BspUartDma_Transmit()`：启动异步发送，要求缓冲区在完成回调前保持有效。
- `BspUartDma_TxComplete()`：正常完成后释放 busy。
- `BspUartDma_TxError()`：错误后释放 busy，允许下一帧恢复。
- `BspUartDma_IsBusy()`：查询当前发送占用状态。

### `Core/bsp/Src/bsp_uart_dma.c`

使用静态 `g_tx_busy` 防止新帧覆盖正在 DMA 读取的缓冲区。启动 HAL 失败时立即回滚 busy；
完成和错误回调都能释放状态，避免通信永久锁死。

### `Core/bsp/Inc/h503_flash.h`

对外只提供安全读取和保留式局部更新，不再公开裸擦除、裸写入接口：

- `H503Flash_Read()`：按参数 Sector 内 offset 读取指定范围。
- `H503Flash_UpdatePreserve()`：要求调用者传入 8 KB RAM 备份区，完整执行读、改、擦、写和整区校验。
- `H503_FLASH_SECTOR_BACKUP_SIZE`：调用者备份缓冲区最小长度，固定为 8 KB。

### `Core/bsp/Src/h503_flash.c`

- `range_valid()`：使用减法形式检查 offset/length，避免无符号加法溢出。
- `erase_config_sector()`：内部擦除固定参数 Sector，并扫描完整 8 KB 是否为 `0xFF`。
- `write_config_sector()`：将 8 KB RAM 镜像按 16 字节 Quad-word 写入；每次先复制到 32 位对齐缓冲。
- `H503Flash_Read()`：通过存储器映射把指定范围复制到 RAM。
- `H503Flash_UpdatePreserve()`：先保存完整 Sector，在 RAM 中修改目标字段；内容未变化则跳过擦写；
  否则擦除完整 Sector、恢复完整镜像并比较整个 8 KB。该接口避免清标志时误删同 Sector 的
  系统参数或校准参数，但不能解决擦除到写回期间掉电造成的数据丢失。
## Dri 器件驱动层

### `Core/dri/Inc/mlx90393.h`

定义 `Mlx90393Config` 可调配置、`Mlx90393Sensitivity` 三轴灵敏度、`Mlx90393` 上下文和
`Mlx90393Sample` 原始样本。

- `Mlx90393_Init()`：接收应用层配置结构体，复位、配置并回读 Reg0/1/2。
- `Mlx90393_GetSensitivity()`：根据上下文中的 HALLCONF、GAIN_SEL 和各轴 RES 查询 `uT/LSB`。
- `Mlx90393_StartSingle()`：启动一次 XYZT 转换。
- `Mlx90393_Read()`：读取状态、温度和 XYZ。
- `Mlx90393_ReadRegister()`：执行 RR。
- `Mlx90393_WriteRegister()`：执行 WR。

### `Core/dri/Src/mlx90393.c`

- `g_sensitivity_hallconf_c`、`g_sensitivity_hallconf_0`：数据手册两套 8x4 灵敏度表。
- `config_valid()`：检查 GAIN、HALLCONF、RES、滤波、OSR、OSR2 和 TCMP 的合法范围。
- `build_reg0_config()`、`build_reg2_config()`：从配置结构体生成寄存器值，避免应用层操作位域。
- `Mlx90393_GetSensitivity()`：分别按 X/Y/Z 的 RES 查表，不假定三轴分辨率相同。
- `command_status()`：内部发送只返回状态的命令，并检查 ERROR 位。
- `Mlx90393_ReadRegister()`：发送 `0x50 + reg<<2`，按大端还原 16 位寄存器。
- `Mlx90393_WriteRegister()`：发送 `0x60 + data + reg<<2`，读取命令状态。
- `Mlx90393_Init()`：检查并保存调用者配置；先 EX，再按手册以“只写后 STOP”方式发 RT；
  按数据手册第 29 页保留
  Reg0 高 8 位 `ANA_RESERVED_LOW/BIST`，在低 8 位设置 `GAIN_SEL/HALLCONF`，关闭 TCMP，设置
  分辨率/滤波/OSR，并逐项回读。通信失败保留原始错误码，只有值不一致才返回
  `APP_STATUS_VERIFY`。
- `Mlx90393_StartSingle()`：发送 `0x3F`，启动 T、X、Y、Z。
- `Mlx90393_Read()`：发送 `0x4F`，读取 9 字节并还原 XYZT。

驱动只处理器件协议，不知道器件是主磁传感器还是干扰参考。

项目默认参数以 `#define` 集中在 `Core/app/Inc/sensor_config.h`，当前为 GAIN=5、HALLCONF=0xC、
三轴 RES=1、DIG_FILT=2、OSR=0、OSR2=0、TCMP=0；`sensor_config.c` 的 `g_sensor_mlx_config`
只引用这些宏。当前链路只上传原始位模式，灵敏度查询接口暂不参与组帧。

### `Core/dri/Inc/lsm6dsow.h`

定义 `Lsm6dsow` 上下文、温度/六轴原始样本以及两个公开接口：

- `Lsm6dsowConfig`：三个字段即 `CTRL1_XL/CTRL2_G/CTRL3_C` 寄存器字节。
- `Lsm6dsow_Init()`：身份检查，并按传入的 `Lsm6dsowConfig` 写入运行配置。
- `Lsm6dsow_Read()`：一次读取温度、三轴陀螺和三轴加速度。

### `Core/dri/Src/lsm6dsow.c`

- `Lsm6dsow_Init()`：读取 `WHO_AM_I`，确认 `0x6C`；按传入的 `Lsm6dsowConfig` 写入
  `CTRL1_XL/CTRL2_G/CTRL3_C`。默认配置（`sensor_config.h` 的宏）为加速度 104 Hz/16 g、
  陀螺 104 Hz/2000 dps，以及 BDU 和寄存器地址自增。
- `Lsm6dsow_Read()`：从 `OUT_TEMP_L` 连续读取 14 字节，按寄存器小端顺序还原温度、陀螺
  XYZ 和加速度 XYZ 共七个 `int16_t`。

## Mid 中间层

### 当前参与构建：`Core/mid/Inc/sensor_protocol.h`

定义 `SensorProtocolInput` 原始快照、24 字节载荷长度和当前默认 40 字节帧长。快照包含时间戳、
当前 `GAIN_SEL` 和三颗 MLX 样本，不包含帧序号。

- `SensorProtocol_Encode()`：把原始快照显式编码为大端字节流，成功返回当前方案帧长，参数、容量、
  载荷长度或最终偏移异常时返回 0。

### 当前参与构建：`Core/mid/Src/sensor_protocol.c`

- `put_u16_be()`：以大端顺序写入一个 16 位整数并推进偏移。
- `put_u32_be()`：以大端顺序写入一个 32 位整数并推进偏移。
- `SensorProtocol_Encode()`：写入 `1A 2B` 帧头、传感器域、传感器 id、传感器类型、GAIN_SEL、长度和时间戳；三颗 MLX 按
  TXYZ 平铺；最后计算单字节 CRC8-CCITT，
  CRC 覆盖帧头至载荷末字节，再追加 `0x3C` 帧尾。

当前协议不包含浮点数、姿态角、双 IMU 差值或磁干扰判断结果。

### 暂不参与构建：`Core/mid/Inc/orientation.h` 与 `Core/mid/Src/orientation.c`

这是前期互补滤波姿态算法草稿。当前 `sensor_app.c` 和协议均不引用该模块，CMake 也不编译它。
待后续确定 IMU 安装方向、坐标系、零偏和算法部署位置后再评估启用。

### 暂不参与构建：`Core/mid/Inc/magnetic_interference.h` 与 `Core/mid/Src/magnetic_interference.c`

这是前期双参考 MLX 阈值诊断草稿。当前固件仅输出 U6 地址 `0x0D`、U8 地址 `0x0E` 的原始 XYZ，不执行
场强换算或阈值判断。待采集整机无干扰和目标干扰源数据后，再在上位机或新固件版本中实现。
## App 应用层

### `Core/app/Inc/sensor_config.h` 与 `Core/app/Src/sensor_config.c`

独立声明和定义项目传感器参数。所有可调项以 `#define` 集中在 `sensor_config.h`：MLX 的 GAIN、
HALLCONF、RES、DIG_FILT、OSR、OSR2、TCMP，以及 LSM 的 ODR/量程等。`sensor_config.c` 的
`g_sensor_mlx_config`（三颗 MLX 共用）和 `g_sensor_imu_config`（两颗 IMU 共用）只引用这些宏。
调参时只编辑 `sensor_config.h` 的宏，无需修改 `sensor_config.c`、驱动或采集流程。

### `Core/app/Inc/sensor_app.h`

声明原始采集生命周期接口：

- `SensorApp_Init()`：初始化当前方案启用的传感器和采样调度器。
- `SensorApp_Process()`：周期读取原始数据并在 DMA 空闲时发送。
- `SensorApp_OnUartTxComplete()`：转发 UART DMA 完成事件。
- `SensorApp_OnUartError()`：转发 UART/DMA 错误事件。

### `Core/app/Src/sensor_app.c`

- `start_all_mlx()`：启动三颗 MLX 的下一轮单次转换，并记录逐颗成功位。
- `g_sensor_mlx_config` / `g_sensor_imu_config`：由独立配置模块统一维护，字段全部引用
  `sensor_config.h` 的 `#define`，分别是三颗 MLX 与两颗 IMU 共用的默认采样配置。
- `SensorApp_Init()`：清空原始快照，初始化当前方案启用的传感器，启动首轮 MLX 转换并设置 deadline。
- `SensorApp_Process()`：处理 tick 回绕和滞后重排；读取上一轮 MLX 原始数据、启动下一轮转换、
  指腹磁铁方案把 USART1 IN 口合法帧和本机帧放入同一个 OUT 队列，由发送泵按先到先发启动 UART DMA。
- `SensorApp_OnUartTxComplete()`、`SensorApp_OnUartError()`：释放 BSP 软件 busy 状态。

应用层不维护姿态状态、不计算磁场模长，也不生成任何算法判断结果。
## CubeMX 文件中的连接点

这些文件不是自定义业务模块，但包含必要 USER CODE 连接：

- `Core/Src/main.c`：集中定义外设句柄和初始化函数；TIM2 配置为 `PSC=2399`、`ARR=199`，
  外设初始化后先启动 500 Hz 时间基准，再初始化传感器；主循环轮询应用，并在 UART 和
  `HAL_TIM_PeriodElapsedCallback()` 中转发 DMA、错误和 TIM2 更新事件。
- `Core/bsp/Inc/bsp_handles.h`：向 BSP 声明集中式 `main.c` 中的 I2C、LPUART、TIM2 和 DMA HAL 句柄。
- `Core/Src/stm32h5xx_hal_msp.c`：配置 GPDMA1 Channel 0、LPUART1 TX DMA 和 I2C1 EV/ER 中断。
- `Core/Src/stm32h5xx_it.c`：转发 DMA 和 I2C 中断给 HAL。
- `STM32H503xx_FLASH.ld`：将应用 Flash 限制为 120 KB，隔离末尾配置 Sector。

