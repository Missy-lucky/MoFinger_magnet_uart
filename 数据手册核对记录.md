# 数据手册核对记录

本文记录当前固件配置与项目 `docs` 中器件资料的对应关系，便于后续评审、改参数和上板排查。
页码采用 PDF 文件显示的文档页码。

## MLX90393

依据 `docs/mlx90393datasheet.pdf`，Revision 007，2021-01-14：

- 第 20 页命令表：SB 为 `0001zyxt`，SM 为 `0011zyxt`，RM 为 `0100zyxt`，RR 为 `0x50`，
  WR 为 `0x60`，EX 为 `0x80`，RT 为 `0xF0`。当前默认 burst 分支测量温度和 XYZ，因此使用
  SB `0x1F`、RM `0x4F`；旧 10 ms 分支仍使用 SM `0x3F`、RM `0x4F`。
- 第 21 页：状态字 bit7 为 BURST_MODE，bit4 为 ERROR；RM 数据顺序为 Status-T-X-Y-Z。
  当前驱动读取 9 字节并按该顺序还原。
- 第 25 至 26 页：I2C 命令写入后通过 repeated START 切换到读方向；RR 地址必须左移两位；
  WR 数据顺序为命令、高字节、低字节、地址左移两位。当前 BSP 顺序事务与驱动字节顺序一致。
- 第 23 页和第 26 页对 RT 的示意图存在不一致：SPI 图和命令表为 `0xF0`，I2C 图文字显示
  `0x80`。由于 `0x80` 同时明确属于 EX，当前实现采用命令表和参考实现一致的 `0xF0`。
- 第 29 页寄存器图：Reg0 的低 8 位包含 Z_SERIES、GAIN_SEL、HALLCONF，高 8 位包含
  ANA_RESERVED_LOW/BIST。当前驱动保留高 8 位，并在低 8 位写 `GAIN_SEL=5`、`HALLCONF=0xC`。
  `Core/Bsp_example` 中相反的位域方向未被沿用。
- 第 29 至 30 页：Reg1 包含 `BURST_SEL`、`BURST_DATA_RATE` 和 bit10 `TCMP_EN`；Reg2 包含
  OSR2、RES_Z/Y/X、DIG_FILT 和 OSR。当前配置为 `BURST_SEL=0x0F`、`BURST_DATA_RATE=0`
  连续 burst、`TCMP_EN=0`、`OSR2=0`、三轴 `RES=1`、`DIG_FILT=2`、`OSR=0`。
- 第 32 页灵敏度表：同一 `HALLCONF=0xC`、`GAIN_SEL=5` 下，RES 从 `0` 改为当前的 `1` 后，
  XY 灵敏度由 `0.250` 变为 `0.501 uT/LSB`，Z 由 `0.403` 变为 `0.807 uT/LSB`。
- 第 33 页转换时间表：OSR `0`、DIG_FILT `2`、OSR2 `0` 的 TXYZ 典型转换时间为 `1.84 ms`，
  最大输出速率约 `493.0 Hz`。当前 1000 Hz 固定发帧会复用上一份有效样本产生重复帧；
  关宏回旧分支时仍满足 10 ms 流水周期。
- 第 32 页灵敏度表：驱动保留 `HALLCONF=0xC` 和 `HALLCONF=0x0` 两套 8x4 查找表，第一维
  为 `GAIN_SEL`，第二维为各轴 `RES`。当前默认配置查询结果为 XY `0.501 uT/LSB`、
  Z `0.807 uT/LSB`，但原始上报链路不执行物理量换算。

## LSM6DSOW

依据 `docs/IMU.pdf`：

- 第 66 页：WHO_AM_I 固定为 `0x6C`，当前初始化会读取并校验。
- 第 67 页：CTRL1_XL 的 `ODR_XL=0100` 为 104 Hz，`FS_XL=01` 在默认 XL_FS_MODE 下为
  正负 16 g，因此当前写入 `0x44`。
- 第 68 页：CTRL2_G 的 `ODR_G=0100` 为 104 Hz，`FS_G=11` 为正负 2000 dps，FS_125 清零，
  因此当前写入 `0x4C`；该量程标称灵敏度为 70 mdps/LSB；当前协议仍输出寄存器原始值，由上位机按需换算。
- 第 69 页：CTRL3_C bit6 为 BDU、bit2 为 IF_INC，因此当前写入 `0x44`，支持连续读取期间的
  数据一致性和寄存器地址自增。
- 第 77 页起的输出寄存器映射：`OUT_TEMP_L/H` 位于 `0x20/0x21`，其后依次为陀螺和加速度
  六轴输出。当前从 `0x20` 连续读取 14 字节，直接输出温度、陀螺和加速度原始 `int16_t`。

## STM32H503 Flash

依据 `docs/stm32h503datasheet.pdf` 第 17 页和工程内 STM32H5 HAL：

- 器件包含 128 KB 双 Bank Flash，每 Bank 8 个 8 KB Sector。
- 最后一个 Sector 为 Bank 2 Sector 7，地址 `0x0801E000` 至 `0x0801FFFF`。
- 链接脚本只允许应用使用前 120 KB，防止链接产物覆盖配置区。
- H5 HAL Quad-word 编程单位为 128 bit；目标地址必须 16 字节对齐，数据地址至少 32 位对齐。
  当前写接口使用 `uint32_t[4]` RAM 缓冲、尾块补 `0xFF`、写前擦除态检查和写后回读校验。

## 仍需实测

- 用逻辑分析仪确认 MLX 的 EX、RT、RR、WR、SB、RM 波形和 repeated START；关宏回旧分支时
  仍确认 SM、RM 波形。
- 读取并记录三颗 MLX 的 Reg0/1/2 回读值，确认实际器件版本与 Revision 007 位域一致。
- 采集无干扰、正常触摸和目标磁干扰源数据，重新确定磁干扰阈值及逐颗校准参数。
- 后续姿态算法开发前，静置和多姿态旋转确认双 IMU 安装方向、坐标系和零偏。
- 对 Flash 执行擦除、非整 Quad-word 写入、越界拒绝、非擦除态拒绝和断电恢复测试。
- 以 921600 baud 接收当前默认 40 字节原始数据帧，校验大端长度、GAIN_SEL 状态、载荷顺序、帧尾和
  CRC8-CCITT；标准测试向量应满足 `123456789 -> 0xF4`。

