# Mosense AY01JA 嵌入式项目知识手册

> 面向读者：刚开始学习 C 语言、STM32 和传感器通信的嵌入式初学者  
> 对应工程：STM32H503KBU6 灵巧手指尖原始传感器采集固件  
> 核对日期：2026-07-30  
> 说明：本文以当前 `CMakeLists.txt` 实际参与编译的源码和 `main.c` 实际调用链为准。

## 1. 先用一句话理解这个项目

这块传感器板使用一颗 STM32H503KBU6，当前默认通过一条 I2C 总线周期读取 3 颗 MLX90393 三轴磁传感器，把原始数据组成 40 字节数据帧，再通过 LPUART1 和 DMA 以 921600 baud 发给上位机。指腹磁铁方案还会把 USART1 作为 IN 口接收下一级合法帧并转发。

它当前是一个“可靠的数据采集节点”，不是一个已经完成姿态解算、磁干扰消除和力学计算的智能算法节点。

可以把它类比成一个快递分拣站：

1. 传感器产生原始数据，相当于货物。
2. I2C 驱动负责把货物取回来。
3. 应用层决定什么时候取、按什么顺序取。
4. 协议层把数据放进有固定格式的箱子。
5. CRC 给箱子加一个运输校验标记。
6. UART DMA 把箱子发给上位机。

当前固件不负责解释这些数据对应多大力、什么姿态或是否受到干扰。这些含义需要标定和算法支持，目前应由上位机处理。

## 2. 硬件组成与连接关系

### 2.1 主控制器 STM32H503KBU6

STM32 是整个系统的控制中心。当前工程使用的关键资源如下：

| 资源 | 当前配置 | 在项目中的用途 |
| --- | --- | --- |
| CPU | Arm Cortex-M33，系统时钟 240 MHz | 执行初始化、调度、驱动和组帧代码 |
| Flash | 128 KB，双 Bank | 保存程序；最后 8 KB保留为参数区 |
| RAM | 32 KB | 保存全局状态、传感器上下文和发送缓冲区 |
| I2C1 | PB6=SCL，PB5=SDA，400 kHz | 同时连接 3 颗 MLX90393 和 2 颗 LSM6DSOW |
| LPUART1 | PA3=TX，PA2=RX，921600，8N1 | 当前原始数据上行接口 |
| GPDMA1 CH0 | LPUART1 TX 请求 | 在后台搬运 UART 发送数据 |
| TIM2 | 500 Hz 更新中断 | 产生每 2 ms 增加一次的业务时间基准 |
| ICACHE | 1-way | 缓存指令，提高 Flash 取指效率 |
| SysTick | HAL 默认 1 ms tick | HAL 内部超时使用，不是应用采样调度基准 |

`USART1`、`USART2` 和 `TIM3` 也在 CubeMX 生成的 `main.c` 中完成了初始化，但是当前应用层没有调用它们。理解“初始化了”和“业务实际使用了”之间的区别非常重要。

### 2.2 三颗 MLX90393

MLX90393 是数字式三轴磁力计，可以测量 X、Y、Z 三个方向的磁感应强度，同时还可以输出芯片温度原始值。

| 数组索引 | I2C 7 位地址 | 当前板级角色 |
| ---: | ---: | --- |
| 0 | `0x0C` | 主磁传感器 |
| 1 | `0x0E` | U6，磁干扰参考传感器 1 |
| 2 | `0x0D` | U8，磁干扰参考传感器 2 |

三个器件共用 SDA/SCL，靠地址区分。地址由芯片 A0/A1 硬件绑带决定，不是软件随意编出来的。

### 2.3 两颗 LSM6DSOW

LSM6DSOW 是六轴惯性测量单元 IMU，内部包含：

- 三轴加速度计：测量线性加速度，也会感受到重力。
- 三轴陀螺仪：测量绕三个轴的角速度。
- 温度传感器：主要帮助理解芯片温漂，不等同于高精度环境温度计。

| 数组索引 | I2C 7 位地址 | 地址来源 |
| ---: | ---: | --- |
| 0 | `0x6A` | SDO/SA0 拉低 |
| 1 | `0x6B` | SDO/SA0 拉高 |

### 2.4 为什么多颗器件可以共用一条 I2C

I2C 是带地址的共享总线。主机先发送目标从机地址，只有地址匹配的从机响应 ACK。只要地址不冲突，总线上可以挂多个设备。

共享总线也意味着多颗器件不能真正同时传输：MCU 必须逐颗访问。因此本项目中的“同步”是同一 10 ms 批次共用一个时间戳，而不是多颗芯片在同一微秒完成采样。

## 3. 从复位到运行：程序是怎样启动的

### 3.1 启动文件和链接脚本

上电或复位后，CPU 首先从向量表读取：

- 初始栈顶地址；
- `Reset_Handler` 地址。

`startup_stm32h503xx.s` 中的 `Reset_Handler` 会准备 C 运行环境，例如把 Flash 中的已初始化数据复制到 RAM 的 `.data` 段、把 `.bss` 清零，然后调用 `SystemInit()` 和 `main()`。

链接脚本 `STM32H503xx_FLASH.ld` 决定代码、只读常量、全局变量和栈分别放到哪里。链接脚本不是普通业务代码，但它决定“程序能放在哪些物理地址”。当前应用 Flash 长度限制为 120 KB，最后 8 KB不会被链接器放入程序内容。

### 3.2 `main()` 的执行顺序

当前 `Core/Src/main.c` 的核心顺序是：

```text
复位
  -> MPU_Config()
  -> HAL_Init()
  -> SystemClock_Config()
  -> GPIO / I2C1 / ICACHE / UART / TIM 初始化
  -> BspTime_Init()
  -> SensorApp_Init()
  -> while (1)
       -> SensorApp_Process()
```

这些步骤不能随意交换。例如传感器初始化需要 I2C 已经工作，业务定时需要 TIM2 已经初始化，DMA 发送需要 UART 和 GPDMA 已经配置。

### 3.3 裸机超级循环

工程当前没有 RTOS。`while (1)` 称为超级循环，CPU 不断调用 `SensorApp_Process()`。这个函数不是每调用一次就采样，而是先检查当前时间是否到达 deadline，没到就立即返回。

这种设计的优点是简单、RAM 开销小、控制路径容易理解；缺点是随着功能增多，任务优先级、阻塞时间和最坏执行时间会越来越难管理。

## 4. 代码架构：每一层解决什么问题

当前有效分层为：

```text
APP  应用编排层
 |       决定何时采、采谁、如何形成一次完整业务周期
 v
MID  中间与协议层             DRI 具体器件驱动层
 |       负责组帧、字节序       | 负责 MLX90393、LSM6DSOW 命令
 v                              v
COM  通用算法层              BSP 板级支持层
 |       CRC、统一状态码         | I2C、UART DMA、时间、Flash
 +---------------+--------------+
                 v
          STM32 HAL / CMSIS
                 v
              寄存器和硬件
```

### 4.1 APP：应用层

主要文件：

- `Core/app/Src/sensor_app.c`
- `Core/app/Src/sensor_config.c`

应用层知道当前方案启用的传感器，也知道采样周期为 10 ms。它保存整个固件生命周期中的静态对象：

- `g_mlx[3]`：三颗磁传感器上下文；
- `g_imu[]`：仅在显式打开 IMU 输出时才编译的 IMU 上下文；
- `g_protocol_input`：本周期原始数据快照；
- `g_tx_frame[96]`：DMA 发送缓冲区；
- `g_next_sample_tick`：下一次采样 deadline；
- `g_measurement_started_mask`：哪些 MLX 成功启动了转换。

这里没有 `malloc()`。静态分配让 RAM 用量在编译时可预测，也避免动态内存碎片，适合小型裸机系统。

`sensor_config.c` 集中保存 MLX 参数。修改增益、分辨率、数字滤波或过采样时不需要进入器件驱动，这体现了“机制与策略分离”：驱动提供能力，应用选择参数。

### 4.2 MID：协议层

主要文件：`Core/mid/Src/sensor_protocol.c`。

它把 C 结构中的整数逐字节写进协议缓冲区。代码没有直接 `memcpy` 整个结构体，因为 C 结构体可能有编译器填充，CPU 本机字节序也可能与通信协议不同。

`orientation.*` 和 `magnetic_interference.*` 虽然还在目录中，但当前未加入根 `CMakeLists.txt`，也没有被应用调用，所以不是当前固件行为的一部分。

### 4.3 DRI：器件驱动层

主要文件：

- `Core/dri/Src/mlx90393.c`
- `Core/dri/Src/lsm6dsow.c`

这一层理解“某颗芯片的数据手册”：寄存器地址、命令格式、字节顺序、身份码和配置位。它不应该理解“这颗 MLX 是主传感器还是参考传感器”，因为那是板级和业务角色。

### 4.4 BSP：板级支持层

主要文件：

- `board_config.*`：板上器件地址、数量、采样周期和 Flash 区域；
- `bsp_i2c.*`：把通用器件事务绑定到 `hi2c1`；
- `bsp_uart_dma.*`：管理异步发送及 busy 状态；
- `bsp_time.*`：向上提供统一毫秒时间；
- `h503_flash.*`：安全维护最后一个 Flash Sector。

BSP 的价值在于隔离板级差异。将来换一块板，可能引脚、I2C 实例和 UART 实例不同，但器件驱动的命令协议不必跟着重写。

### 4.5 COM：公共层

主要内容是统一 `AppStatus` 和 CRC8。自定义驱动不直接向上层暴露 `HAL_StatusTypeDef`，这样上层不会和 ST HAL 紧密耦合。

### 4.6 HAL、CMSIS 和寄存器层

- HAL 是 ST 提供的高级外设库，例如 `HAL_I2C_Mem_Read()`。
- CMSIS 统一 Cortex-M 内核、异常、中断和寄存器定义。
- 最底层是内存映射寄存器。写某个地址上的特定位，就会改变真实外设电路状态。

抽象层不是让硬件消失，而是把复杂细节封装到合适的位置。

## 5. 一次 10 ms 采样周期的详细流程

### 5.1 初始化阶段

`SensorApp_Init()` 执行：

1. 清空协议快照和调度状态。
2. 按地址表依次初始化三颗 MLX90393。
3. 按地址表依次初始化两颗 LSM6DSOW。
4. 给全部初始化成功的 MLX 启动第一次单次转换。
5. 设置第一个 10 ms deadline。

单颗器件初始化失败不会让整个系统停机。驱动上下文中的 `initialized` 保持 0，后续访问会返回 `APP_STATUS_NOT_READY`。

### 5.2 周期调度

核心判断是：

```c
if ((int32_t)(now - g_next_sample_tick) < 0) return;
```

把两个无符号 tick 的差转换为有符号数，是嵌入式系统常见的回绕安全比较方式。即使 `uint32_t` 毫秒计数约 49.7 天后从 `0xFFFFFFFF` 回到 0，短时间范围内的 deadline 比较仍然有效。

正常情况下用 `g_next_sample_tick += 10` 保持稳定周期，避免每次执行耗时都累积到周期中。如果主循环严重滞后，则从当前时刻重新排期，避免为了补历史周期而连续猛采导致 I2C 拥塞。

### 5.3 MLX 流水式单次测量

MLX 采用“上一轮转换与本轮其他工作重叠”的方式：

```text
初始化：启动转换 A
第 1 周期：读取 A -> 启动 B -> 读取 IMU -> 组帧发送
第 2 周期：读取 B -> 启动 C -> 读取 IMU -> 组帧发送
```

当前 TXYZ 典型转换时间约 1.84 ms，小于 10 ms 周期，所以到下一次读取时通常已经完成。这样没有在采样函数中原地等待 1.84 ms。

`g_measurement_started_mask` 的每一位对应一颗 MLX。只有本轮启动成功，下一轮才读取它，避免把没有新转换的数据误认为新样本。

### 5.4 IMU 连续读取

每颗 IMU 从 `OUT_TEMP_L` 开始连续读取 14 字节：

```text
温度 2 字节
陀螺 X/Y/Z 6 字节
加速度 X/Y/Z 6 字节
```

芯片输出寄存器是小端，因此低字节在前。驱动还原为 `int16_t` 后，协议层再按上行协议要求转换成大端。

### 5.5 失败样本如何处理

每个周期开始时，应用先把 MLX 和 IMU 样本数组清零。某颗器件读取失败时，本周期对应字段保持 0，不会沿用上次成功值。

但当前 40 字节协议没有每颗器件独立的有效位。这意味着上位机仅看到 0 时，无法严格区分“真实原始值恰好为 0”和“本次读取失败”。这是当前协议的限制，后续版本可增加有效位图、错误计数或序号。

### 5.6 DMA 忙时怎么办

应用先采集，再检查 DMA：

- DMA 空闲：编码并启动发送；
- DMA 忙：不覆盖旧缓冲区，直接丢弃本次待发送帧；
- 不论是否发送，本轮采集和下一轮 MLX 转换仍继续。

这是一种“新帧可丢，旧帧不能损坏”的实时策略。对连续高频传感器流通常比阻塞等待更合理。

## 6. I2C 从电气层到底层驱动

### 6.1 电气原理：开漏与上拉

I2C 的 SDA 和 SCL 通常是开漏输出。设备能主动拉低总线，但不能主动推高；释放后由外部上拉电阻把电平拉高。

这样多个设备共享总线时不会出现一个器件强推高、另一个强拉低造成的大电流冲突。代价是上升沿由上拉电阻和总线电容共同决定：

```text
上升时间大致与 Rpull-up × Cbus 成正比
```

上拉太大或总线电容太大，会导致高电平上升过慢，在 400 kHz 下可能读错；上拉太小则低电平电流增大。排查 I2C 问题时，逻辑分析仪看协议，示波器看电气波形，两者作用不同。

### 6.2 基本时序

- 空闲：SCL、SDA 都为高。
- START：SCL 为高时 SDA 从高变低。
- 数据：通常在 SCL 低电平期间改变，在高电平期间采样。
- ACK：接收方在第 9 个时钟把 SDA 拉低。
- NACK：第 9 个时钟未拉低。
- STOP：SCL 为高时 SDA 从低变高。
- repeated START：不先发 STOP，直接开始下一段事务，保持对总线控制。

7 位地址在总线上会与读写位组合。HAL 接口经常要求传入 `address << 1`，本项目 BSP 负责这层转换，应用层和器件层保存的仍是更容易阅读的 7 位地址，例如 `0x0C`。

### 6.3 两类器件的协议差异

LSM6DSOW 使用典型“寄存器地址”模型：先写寄存器地址，再读或写数据，所以适合 `HAL_I2C_Mem_Read/Write()`。

MLX90393 更像“命令处理器”：发送 `SM`、`RM`、`RR`、`WR` 等命令，有些命令要求写命令后 repeated START 读取响应。不能假设所有 I2C 芯片都使用同一种寄存器访问形式。

### 6.4 当前 BSP 的事务类型

`bsp_i2c.c` 提供：

- 普通主机写；
- 命令后 repeated START 读；
- 命令加参数后读；
- 多字节写命令后读取状态；
- 标准 8 位寄存器读写。

MLX 的 WR 使用 HAL 顺序中断事务，在发送阶段使用 `I2C_FIRST_FRAME`，接收阶段使用 `I2C_LAST_FRAME`，由 I2C1 EV/ER 中断推进状态机并产生 repeated START。超时后还会 abort，尽量让句柄恢复到可继续工作的状态。

### 6.5 常见 I2C 故障

| 现象 | 常见原因 | 建议检查 |
| --- | --- | --- |
| 所有地址都 NACK | 无供电、无共地、引脚复用错误、SCL/SDA 接反 | 电源、GPIO AF、上拉、示波器 |
| 只有某颗 NACK | 地址绑带不符、该器件焊接或供电异常 | 扫描地址、原理图、器件局部波形 |
| 偶发超时 | 上升沿慢、干扰、事务状态未恢复、优先级问题 | 波形、HAL ErrorCode、总线频率 |
| SDA 长期为低 | 从机中途掉电或事务中断，保持在未完成状态 | 复位从机、产生恢复时钟、检查掉电时序 |
| 读到固定 `0xFF` | 总线浮空、读失败后缓冲区未更新、地址不对 | ACK、返回码、初始化缓冲区 |

## 7. MLX90393 的工作原理与驱动实现

### 7.1 磁传感器测到了什么

磁感应强度通常以特斯拉 T 表示，工程中常用 mT 或 uT。MLX90393 内部利用霍尔效应感知三个方向的磁场，再经过模拟前端、模数转换和数字处理，输出整数原始码。

原始码不是天然的 uT。大致换算关系为：

```text
B_axis[uT] = raw_axis[LSB] × sensitivity_axis[uT/LSB]
```

灵敏度取决于 `HALLCONF`、`GAIN_SEL` 和每个轴的 `RES`。本项目驱动保存了数据手册标称表，但当前协议仍上传原始值，没有在 MCU 内做浮点换算。

### 7.2 初始化过程

`Mlx90393_Init()` 的主要步骤：

1. 校验配置字段范围。
2. 发送 EX，退出可能存在的 Burst/WOC/SM 状态。
3. 延时 2 ms。
4. 发送 RT 软复位命令，并以 STOP 结束。
5. 延时 2 ms。
6. 读取 Reg0 和 Reg1，以保留不应盲目覆盖的位。
7. 根据配置构造 Reg0、Reg1、Reg2。
8. 逐个写寄存器。
9. 逐个回读比较。
10. 全部成功后设置 `initialized=1`。

写后回读可以发现“通信函数返回成功，但寄存器实际值不符合预期”的问题。返回 `APP_STATUS_VERIFY` 与纯通信错误分开，有利于定位。

### 7.3 当前配置的意义

```text
HALLCONF = 0xC
GAIN_SEL = 5
RES_X/Y/Z = 1
DIG_FILT = 2
OSR = 0
OSR2 = 0
TCMP_EN = 0
```

- 增益越高通常每个 LSB 代表的磁场越小，分辨能力更细，但可测范围更容易饱和。
- RES 会改变输出分辨率及换算系数。
- 过采样和数字滤波通常能降低噪声，但增加转换时间，降低最高采样率。
- 温度补偿开关会改变器件内部处理方式，不能只看名字就假设效果，需结合数据手册和实测标定。

“精度、量程、噪声、带宽、延迟”往往相互制约，不存在所有指标同时无限提高的配置。

### 7.4 主传感器与参考传感器

代码把索引 0 定义为主磁传感器，把另外两颗定义为干扰参考。这个角色目前只体现在地址表顺序，固件没有执行主传感器减参考传感器等算法。

未来若做磁干扰补偿，不能简单默认：

```text
有效信号 = 主传感器 - 参考传感器
```

还需考虑安装位置、磁场空间梯度、三轴坐标变换、各器件零偏和增益差、温漂以及目标磁体对参考传感器的耦合。

## 8. LSM6DSOW 的工作原理与驱动实现

### 8.1 MEMS 加速度计

MEMS 加速度计内部可简化理解为微小质量块、弹性结构和电容检测电路。加速度使质量块发生相对位移，电容变化被转换成数字量。

静止放在桌面上时，它仍会测到约 1 g，这是支撑力对应的比力表现。因此仅用加速度计可以估计静态倾角，但在快速运动时，线性加速度会混入重力方向估计。

当前配置为 104 Hz、正负 16 g。数据手册对应的典型灵敏度可用于上位机换算，但当前固件只发 `int16_t` 原始码。

### 8.2 MEMS 陀螺仪

MEMS 陀螺仪通常利用振动结构中的科里奥利效应测量角速度。对角速度积分可得到角度变化：

```text
angle(t) = angle(0) + integral(angular_rate × dt)
```

但零偏的微小误差也会随时间积分，形成漂移。所以仅靠陀螺不能长期保持绝对姿态。

当前配置为 104 Hz、正负 2000 dps。大量程适合快速运动，但单位码对应的角速度较大，细微运动分辨能力相对降低。

### 8.3 BDU 和 IF_INC

- BDU（Block Data Update）保证一组输出的高低字节读取期间不会被新样本拆开更新，避免高字节来自旧样本、低字节来自新样本。
- IF_INC 让寄存器地址自动递增，因此一次 14 字节 burst read 可以取完温度、陀螺和加速度，减少 I2C 起停开销。

### 8.4 104 Hz ODR 与 100 Hz读取

IMU 内部以 104 Hz更新输出寄存器，MCU 每 10 ms 即 100 Hz读取。两者并非同一个时钟源，长期会有相位漂移。当前没有读取 data-ready 引脚，也没有 FIFO 时间戳，所以读取到的是“访问时寄存器中最近一次完成的样本”。

如果未来要求严格同步，应考虑 DRDY 中断、FIFO、传感器时间戳、外部同步信号或统一时钟误差建模。

## 9. TIM2、SysTick、中断与时间

### 9.1 TIM2 频率计算

TIM2 输入时钟为 240 MHz：

```text
计数频率 = 240 MHz / (PSC + 1)
         = 240 MHz / 2400
         = 100 kHz

更新频率 = 100 kHz / (ARR + 1)
         = 100 kHz / 200
         = 500 Hz

更新周期 = 1 / 500 Hz = 2 ms
```

每次 TIM2 更新中断，`BspTime_OnTim2Elapsed()` 把 `g_tim2_tick_ms` 增加 2。因此变量单位仍是毫秒，只是分辨率为 2 ms。

### 9.2 中断的完整调用链

```text
TIM2 硬件计数溢出
 -> NVIC 响应 TIM2_IRQn
 -> TIM2_IRQHandler()
 -> HAL_TIM_IRQHandler(&htim2)
 -> HAL_TIM_PeriodElapsedCallback()
 -> BspTime_OnTim2Elapsed()
 -> g_tim2_tick_ms += 2
```

ISR 应尽量短。当前中断只加一个数，复杂 I2C 采集和组帧仍在主循环执行，这是合理的职责划分。

### 9.3 为什么还有 SysTick

`SysTick_Handler()` 仍调用 `HAL_IncTick()`，所以 HAL 自己的毫秒 tick 正常运行。当前业务层选择 TIM2 作为统一采样时间基准，HAL 阻塞式 API 的内部超时仍可能使用 HAL tick。

“系统中只能有一个时间源”是误解；真正重要的是明确每个时间源服务谁、分辨率是多少、是否会停止以及它们是否需要同步。

### 9.4 中断优先级注意事项

当前 TIM2、UART 的抢占优先级较高，GPDMA 为 1，I2C EV/ER 为 2。数值越小优先级越高。高优先级 ISR 过长会延迟低优先级 I2C 顺序事务，因此以后向回调中增加日志或复杂计算时要格外谨慎。

## 10. UART 和 DMA 的底层原理

### 10.1 UART 8N1

UART 没有共享时钟线，发送方和接收方约定波特率。8N1 表示：

- 1 个起始位；
- 8 个数据位；
- 无校验位；
- 1 个停止位。

每个有效数据字节在线路上占 10 bit。40 字节帧以 100 Hz 发送：

```text
线路位率 = 68 × 10 × 100 = 68000 bit/s
```

占 921600 baud 的约 7.38%，带宽余量很大。一帧理论发送时间约 0.738 ms，小于 10 ms周期。

### 10.2 DMA 为什么有用

不用 DMA 时，CPU 可能需要等待发送寄存器空闲并逐字节写入。DMA 是独立的数据搬运硬件，配置好源地址、目标地址和长度后，可以把 RAM 中的帧送入 UART 数据寄存器。

当前配置：

- 源：RAM 缓冲区，地址递增；
- 目标：LPUART1 发送寄存器，地址固定；
- 宽度：1 字节；
- 模式：normal；
- 完成后产生中断。

### 10.3 缓冲区生命周期

DMA 异步发送意味着 `HAL_UART_Transmit_DMA()` 返回时，数据通常还没有发送完。直到完成回调之前，发送缓冲区都不能被修改。

`g_tx_busy` 正是软件所有权标记：

```text
空闲 -> 设置 busy -> 启动 DMA -> DMA 搬运
     -> 完成/错误中断 -> 回调清 busy -> 可复用
```

这比“函数返回就认为缓冲区可写”更接近真实硬件行为。

## 11. 当前 40 字节通信协议

### 11.1 总体布局

所有多字节字段按大端编码：

| 偏移 | 长度 | 内容 |
| ---: | ---: | --- |
| 0 | 2 | 帧头 `1A 2B` |
| 2 | 1 | 传感器域 `1` |
| 3 | 1 | 传感器 id `0` |
| 4 | 1 | 传感器类型：指尖磁铁方案 `0x01`，指腹磁铁方案 `0x02` |
| 5 | 1 | MLX `GAIN_SEL`，当前为 `5` |
| 6 | 2 | 总帧长 `40` |
| 8 | 4 | 本批次 TIM2 毫秒时间戳 |
| 12 | 2 | 载荷长度 `24` |
| 14 | 24 | 3 颗 MLX，每颗 8 字节 |
| 38 | 1 | CRC8-CCITT |
| 39 | 1 | 帧尾 `3C` |

每颗 MLX 顺序是温度、X、Y、Z。当前默认协议不写入 IMU：指尖磁铁方案暂不上报 IMU，指腹磁铁方案硬件上没有 IMU。

### 11.2 大端、小端与补码

以 `0x1234` 为例：

- 大端传输：`12 34`；
- 小端存储：`34 12`。

有符号 `int16_t` 通常使用二进制补码。协议层先把它转换成 `uint16_t` 再拆字节，只改变 C 类型解释，不改变 16 位位模式，因此负数可被正确传输。

### 11.3 CRC8-CCITT

当前参数：

```text
Poly   = 0x07
Init   = 0x00
RefIn  = false
RefOut = false
XorOut = 0x00
```

CRC 覆盖偏移 0 至 65，不包括 CRC 自身和帧尾。标准测试字符串 `123456789` 的结果应为 `0xF4`。

CRC 能较好检测传输中的随机错误，但它不是加密、签名或身份认证。攻击者可以修改内容并重新计算 CRC。

### 11.4 上位机解析建议

一个健壮的流式解析器应：

1. 在字节流中搜索 `1A 2B`。
2. 等待至少读到长度字段。
3. 检查总长度是否合理且为 68。
4. 收齐整帧。
5. 检查帧尾。
6. 重算 CRC。
7. 按大端解析字段。
8. CRC 失败时只丢弃必要字节并重新找帧头，避免整个连接永久错位。

## 12. Flash、擦写和掉电风险

### 12.1 Flash 和 RAM 的区别

- RAM 读写快，掉电丢失；运行时变量主要放这里。
- Flash 掉电保存，写入和擦除受硬件粒度限制，并有寿命上限。

STM32 内部 Flash 通常不能像 RAM 那样把任意字节从 0 改回 1。擦除先把整个 Sector 变为 `0xFF`，编程再把某些位从 1 写成 0。

### 12.2 当前参数区

```text
地址：0x0801E000 ~ 0x0801FFFF
大小：8 KB
位置：Bank 2 Sector 7
擦除单位：8 KB
编程单位：16 字节 Quad-word
```

链接脚本把应用限制在前 120 KB，从构建阶段阻止程序占用参数区。

### 12.3 保留式更新

`H503Flash_UpdatePreserve()` 的思路是：

1. 把完整 8 KB Sector 读入调用者提供的 RAM 备份区。
2. 在 RAM 镜像中修改目标字段。
3. 若内容没变，直接返回，减少擦写。
4. 解锁 Flash。
5. 擦除完整 Sector。
6. 按 16 字节恢复完整镜像。
7. 回读比较完整 8 KB。
8. 重新锁定 Flash。

它解决了“小字段更新却误删同 Sector 其他字段”的问题，但不能解决擦除后、恢复完成前突然掉电的问题。

### 12.4 更可靠的参数存储扩展

对掉电可靠性要求高时，可使用双槽或日志式设计：

- 两份记录分别带 magic、版本号、长度、序号和 CRC；
- 启动时选择 CRC 有效且序号最新的一份；
- 更新时先完整写入另一槽并校验，最后提交有效标志；
- 不要先破坏唯一有效副本。

还要考虑 Flash 擦写寿命。频繁变化的数据不应每 10 ms写一次 Flash，可以使用 RAM 累积、降低保存频率、轮转页面或外置 EEPROM/FRAM。

## 13. 当前工程中“有但没有运行”的内容

判断一个文件是否真实生效，至少检查三件事：

1. 是否加入构建系统；
2. 是否被活动代码引用；
3. 运行时调用链是否能走到它。

当前情况：

- `Core/com`、`bsp`、`dri`、`mid/sensor_protocol.c`、`app` 中列入根 `CMakeLists.txt` 的文件是活动自定义代码。
- `Core/Bsp_example` 没有加入当前根 CMake，是示例/旧实现，不应拿它解释当前运行行为。
- `orientation.*`、`magnetic_interference.*` 没有加入当前构建，也没有进入应用调用链。
- USART1、USART2、TIM3 已初始化且中断入口存在，但当前没有业务收发或定时逻辑使用它们。
- LPUART1 TX、GPDMA1 CH0、I2C1、TIM2 才在当前核心数据链上。

初学者常见误区是“搜索到一个宏或函数，就认为程序正在用它”。真正可靠的方法是从 `CMakeLists.txt` 和 `main()` 顺着调用链向下追。

## 14. C 语言知识点在本项目中的落地

### 14.1 `static`

文件作用域的 `static` 变量只在当前 `.c` 文件可见，但生命周期贯穿整个程序。`g_tx_frame` 就需要在 DMA 完成前一直存在，所以不能使用一个函数返回后就失效的普通局部数组。

`static` 函数表示只供当前源文件内部使用，例如协议层的 `put_u16_be()`，能减少对外暴露的接口。

### 14.2 `const`

`const Mlx90393Config` 表示函数不应通过该指针修改调用者配置。全局常量通常可放在 Flash，而不是占用可写 RAM。

### 14.3 `volatile`

`g_tim2_tick_ms` 会在中断中改变，主循环也会读取，因此需要 `volatile` 告诉编译器每次都从实际内存读取，不要假设它没有被当前代码路径修改。

但 `volatile` 不自动保证多线程互斥，也不等于内存屏障。这里 32 位对齐读写在 Cortex-M33 上可原子完成，场景较简单。

### 14.4 结构体和上下文

`Mlx90393` 结构体把地址、初始化状态和配置绑定成一个设备上下文。同一套驱动函数可以服务三颗芯片，调用时传不同上下文即可。这比为每颗芯片复制一套函数更易维护。

### 14.5 位运算

寄存器和状态位大量使用：

- `|`：设置位；
- `&`：提取或保留位；
- `~`：构造清位掩码；
- `<<`：把字段移动到指定位置；
- `>>`：取高字节。

例如 `1u << i` 生成第 i 位的掩码，用一个 `uint8_t` 表示三颗 MLX 各自是否成功启动。

### 14.6 错误码

统一的 `AppStatus` 比简单返回 true/false 更有诊断价值。忙、超时、I/O 错误、身份错误、校验失败和未初始化的处理方式并不相同。

当前应用层不少调用使用 `(void)` 丢弃返回值，这是“单器件失败不阻止整体”的容错选择，但也使运行时诊断信息不足。以后可以增加错误计数并通过协议上报，而不必立刻让整个系统停机。

## 15. 实时性、吞吐量和资源估算

### 15.1 I2C 总线不是只有载荷字节

估算 I2C 时间时，不能只计算数据字节。每个字节还需要 ACK 位，事务还有地址、命令、START/repeated START/STOP。400 kHz 下每个 9 bit 字节阶段至少约 22.5 us，实际还会有软件和器件状态开销。

当前 100 Hz负载相对温和，但新增更多传感器或提高采样率前，应使用逻辑分析仪测量一整周期总线占用，而不是只靠理论相加。

### 15.2 周期、延迟和抖动不是一回事

- 周期：相邻采样批次的目标间隔，当前 10 ms。
- 延迟：物理量变化到上位机拿到数据的时间。
- 抖动：周期或延迟相对理想值的变化。
- 吞吐量：单位时间成功传输的数据量。

系统可以平均 100 Hz，但偶尔出现 8 ms、12 ms 间隔；平均频率正确并不代表实时性稳定。

### 15.3 当前同步边界

一个周期只读取一次 `now`，当前启用的本地传感器共享此时间戳。它表达“同一批次”，不表达每颗器件的精确采样瞬间。MLX 结果实际来自上一周期启动的转换。

## 16. 构建系统和 CubeMX 的关系

### 16.1 CubeMX `.ioc`

`Mosense_AY01JA.ioc` 是外设和引脚配置的工程描述。CubeMX 根据它生成 `main.c`、MSP 和中断框架。`USER CODE BEGIN/END` 区域用于保留手写连接代码。

重新生成代码后要复查：

- I2C EV/ER 中断是否仍连接；
- GPDMA 初始化和 UART DMA link 是否仍在；
- TIM2 回调是否仍调用 BSP 时间函数；
- UART 完成/错误回调是否仍释放 busy；
- 根 CMake 自定义源码是否完整；
- 链接脚本是否仍保留最后 8 KB。

### 16.2 CMake 决定实际编译什么

根 `CMakeLists.txt` 的 `target_sources()` 是判断自定义模块是否进入固件的重要依据。目录中存在 `.c` 文件并不意味着它会自动进入最终 ELF。

构建产物通常包括：

- `.elf`：带段、符号和调试信息的可执行映像；
- `.bin`：纯二进制，适合直接烧录指定地址；
- `.hex`：带地址信息的文本格式；
- `.map`：查看各函数和变量占用地址与空间的重要文件。

## 17. 推荐的调试路径

### 17.1 从下到上分层验证

1. 电源与时钟：电压、复位、系统时钟是否正常。
2. GPIO 和物理总线：SCL/SDA 是否有上拉和正确波形。
3. 地址：五个地址是否 ACK，WHO_AM_I 是否为 `0x6C`。
4. 驱动事务：MLX 命令状态、配置回读是否正确。
5. 周期调度：TIM2 tick 和 10 ms deadline 是否稳定。
6. 协议：40 字节、帧头尾、字段偏移和 CRC 是否正确。
7. 上位机：串口参数、流式重同步、大小端和有符号解析。
8. 物理意义：原始值随磁体和运动是否符合预期。

### 17.2 推荐工具

- 调试器：断点、观察变量、查看 HardFault。
- 逻辑分析仪：解码 I2C/UART、测量事务间隔。
- 示波器：看电压质量、上升沿、毛刺和时钟波形。
- 串口抓包：保存原始二进制，不要只看格式化文本。
- map 文件：分析 Flash/RAM 和符号占用。

### 17.3 不要在高频中断里直接 `printf`

格式化输出耗时长，还可能阻塞或依赖锁。它会改变本来要观察的实时行为。更稳妥的方法是累积错误计数、记录轻量事件，在低优先级主循环中批量输出，或使用调试器/ITM。

## 18. 当前设计的优点、限制与可演进方向

### 18.1 优点

- 分层清楚，应用不直接操作寄存器。
- 传感器配置集中，驱动可复用。
- MLX 使用流水转换，减少阻塞等待。
- DMA 缓冲区生命周期受到 busy 保护。
- 单颗传感器失败不阻止其他器件采集。
- 协议显式序列化，不依赖结构体布局。
- Flash 参数区由链接脚本隔离，并提供整 Sector 保留更新。

### 18.2 当前限制

- 协议没有每颗传感器有效位和错误原因。
- 没有帧序号，难以严格统计丢帧。
- UART RX 当前未形成命令协议。
- 传感器初始化失败后没有周期重试或总线恢复策略。
- 当前本地传感器不是硬同步；协议时间戳表达同一采样批次。
- Flash 更新不是掉电原子的。
- 应用层丢弃多数错误码，板上诊断能力有限。
- 初始化了未使用的 USART1、USART2、TIM3，会增加理解和维护负担。
- 当前算法草稿与有效代码同目录，初学者容易误判。

### 18.3 推荐演进顺序

1. 先增加帧序号、有效位图和每类错误计数。
2. 用逻辑分析仪验证 100 Hz 周期、I2C 时序和 DMA 发送间隔。
3. 增加传感器离线后的有限频率重初始化，避免每周期反复阻塞。
4. 根据同步需求评估 IMU DRDY/FIFO 和 MLX 转换就绪检查。
5. 为协议编码和 CRC 增加主机侧单元测试和黄金帧。
6. 参数真正需要持久化时，再设计版本化双槽 Flash 记录。
7. 完成标定数据采集后，再引入姿态融合或磁干扰补偿。

## 19. 可以从本项目扩展学习的知识

### 19.1 传感器标定

常见误差模型包括：

```text
measured = scale_matrix × true_value + bias + temperature_effect + noise
```

- bias：零偏；
- scale：比例误差；
- cross-axis：轴间耦合；
- misalignment：安装轴与理想坐标系不一致；
- temperature effect：随温度变化；
- noise：随机噪声。

标定不是随意调一个系数，而是设计已知输入、采集覆盖范围充分的数据、拟合模型并用独立数据验证。

### 19.2 姿态融合

加速度计短期受运动干扰但可提供重力方向，陀螺短期平滑但长期漂移。互补滤波、扩展卡尔曼滤波等方法利用它们的互补特性。

只有加速度计和陀螺仪时，绕重力方向的绝对 yaw 通常不可观。磁力计可提供航向参考，但本项目的 MLX 很可能主要服务于指尖磁目标测量，并可能受到局部磁体强烈影响，不能未经验证就直接用作电子罗盘。

### 19.3 状态机

随着功能增加，可以把初始化、正常运行、降级、恢复和故障明确建模为状态机，而不是堆叠大量 if：

```text
RESET -> INIT -> RUN
          |       |
          v       v
        RETRY <- DEGRADED
```

每个状态应定义允许事件、超时、重试次数和转换条件。

### 19.4 看门狗

独立看门狗可以在软件卡死时复位 MCU，但正确做法不是在任意位置无条件喂狗。应在确认关键任务都健康、周期执行完成后喂狗，否则系统局部失效时仍可能一直喂狗而不复位。

### 19.5 RTOS 何时值得引入

当前单一采集链用超级循环足够。若增加双向通信、复杂算法、多种周期任务、日志和固件升级，RTOS 的任务、队列和事件可能提高可维护性。

RTOS 不会自动解决实时问题；优先级反转、栈溢出、竞争条件和死锁反而会成为新课题。先理解当前中断与超级循环，再学习 RTOS 会更扎实。

## 20. 建议初学者按这个顺序阅读源码

### 第一遍：只建立全貌

1. `README.md`
2. 根 `CMakeLists.txt`
3. `Core/Src/main.c` 的 `main()` 和三个 HAL 回调
4. `Core/app/Src/sensor_app.c`

目标：能口述“系统何时采、采什么、发到哪里”。

### 第二遍：理解器件和协议

1. `Core/app/Src/sensor_config.c`
2. `Core/dri/Src/mlx90393.c`
3. `Core/dri/Src/lsm6dsow.c`
4. `Core/mid/Src/sensor_protocol.c`
5. `Core/com/Src/com_crc.c`

目标：能从一次函数调用追到总线字节和最终帧字段。

### 第三遍：理解 MCU 底层

1. `Core/bsp/Src/bsp_i2c.c`
2. `Core/bsp/Src/bsp_uart_dma.c`
3. `Core/bsp/Src/bsp_time.c`
4. `Core/Src/stm32h5xx_hal_msp.c`
5. `Core/Src/stm32h5xx_it.c`
6. `Core/bsp/Src/h503_flash.c`
7. `STM32H503xx_FLASH.ld`
8. `startup_stm32h503xx.s`

目标：理解“HAL 调用最终如何对应到引脚、DMA、中断、内存和寄存器”。

## 21. 建议动手实验

每次实验只改变一个变量，并保存串口原始数据和逻辑分析仪截图。

1. 在调试器中观察 `g_tim2_tick_ms` 每次增加 2，验证 TIM2 时间链。
2. 捕获一次 MLX `SM` 和下一周期 `RM`，识别地址、命令、ACK 和 repeated START。
3. 捕获一颗 IMU 的 14 字节 burst read，核对寄存器小端还原。
4. 保存一帧 40 字节数据，手算长度、偏移和 CRC。
5. 暂时断开一颗传感器，观察初始化返回码和该周期字段清零行为。
6. 在上位机统计帧间时间，区分 100 Hz平均频率和抖动。
7. 改变 MLX `GAIN_SEL` 前先预测灵敏度和饱和趋势，再用相同磁体距离对比原始码。
8. 读取 map 文件，找出代码、常量、静态缓冲区分别占多少 Flash/RAM。

不要直接从 Flash 擦写、极限时钟或中断优先级大改开始。先把可观测链路建立起来，实验才容易解释和回退。

## 22. 一份最终心智模型

当你再次打开这个工程，可以按下面的顺序思考：

```text
硬件世界
  磁场、加速度、角速度
        |
        v
传感器内部
  模拟前端 -> ADC -> 数字寄存器
        |
        v
I2C 总线
  地址 -> 命令/寄存器 -> ACK -> 数据
        |
        v
DRI + BSP
  器件语义 -> HAL 事务 -> 外设寄存器
        |
        v
APP
  10 ms 调度 -> 批次时间戳 -> 容错
        |
        v
MID + COM
  字段排序 -> 大端编码 -> CRC
        |
        v
UART + DMA
  RAM 缓冲区 -> 串行线路 -> 上位机
```

学习嵌入式的关键不是记住每个 HAL 函数，而是能在这条链上定位问题：物理量是否产生、芯片是否转换、总线是否成功、驱动是否正确解释、调度是否准时、协议是否无歧义、数据是否完整到达。当前项目已经提供了一条相对清晰的练习路径。

## 23. 术语速查

| 术语 | 简明解释 |
| --- | --- |
| MCU | 集成 CPU、Flash、RAM 和外设的微控制器 |
| BSP | 把软件接口绑定到某块具体板的支持层 |
| Driver/DRI | 按器件数据手册实现命令和寄存器访问 |
| HAL | 芯片厂商提供的硬件抽象库 |
| CMSIS | Arm Cortex-M 软件接口标准 |
| I2C | 两线、带地址、主从式同步串行总线 |
| UART | 无共享时钟的异步串行通信 |
| DMA | 不依赖 CPU 逐字节执行的数据搬运硬件 |
| ISR | 中断服务函数 |
| NVIC | Cortex-M 的中断控制器 |
| ODR | 传感器内部输出数据率 |
| LSB | 数字量最低有效位，也常用作原始码单位 |
| BDU | 防止多字节样本读取时被中途更新 |
| CRC | 用多项式余数检测传输错误的校验方法 |
| Flash Sector | Flash 的擦除基本区域 |
| linker script | 决定程序各段物理地址的链接规则 |
| deadline | 某项周期工作下一次应执行的目标时刻 |
| polling | 主循环反复查询状态 |
| callback | 底层事件完成后反向调用的通知函数 |
